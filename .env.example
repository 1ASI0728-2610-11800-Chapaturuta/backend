# Copy this file to `.env` and fill in the real values.
# `.env` is gitignored — never commit real secrets.
# Keys use the ASP.NET Core double-underscore convention so they bind to
# nested configuration sections (Assistant__ApiKey -> configuration["Assistant:ApiKey"]).

# ── Discovery AI Assistant (OpenRouter) ──────────────────────────────────────
# OpenRouter API key. Get one at https://openrouter.ai/keys
Assistant__ApiKey=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
